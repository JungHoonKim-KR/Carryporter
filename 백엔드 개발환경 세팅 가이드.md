# 🛠️ 백엔드 개발 환경 가이드

이 문서는 로컬 환경에서 백엔드 개발을 진행하기 위한 설정 및 실행 방법을 안내합니다.

## 📋 사전 요구사항 (Prerequisites)

*   **Docker & Docker Compose**: 설치 및 실행 상태여야 합니다.
*   **JDK 17**: 프로젝트는 Java 17 버전을 사용합니다.
*   **IntelliJ IDEA** (또는 Android Studio): 권장 IDE입니다.

---

## 🚀 1. 인프라 실행 (Database, Redis, MQTT)

로컬 개발 시 필요한 미들웨어(MySQL, Redis, Mosquitto)를 Docker 컨테이너로 실행합니다.
배포 환경과 달리, 로컬 개발용 설정(`docker-compose-local.yml`)을 사용하여 포트를 외부에 노출합니다.

1.  **Docker Network 생성 (최초 1회)**
    프로젝트에서 사용하는 Docker 네트워크를 생성합니다.
    ```bash
    docker network create app-network
    ```
    > 이미 존재한다는 에러가 발생하면 무시하고 진행하셔도 됩니다.

2.  **infra 디렉토리로 이동**
    ```bash
    cd infra
    ```

3.  **로컬용 Docker Compose 실행**
    ```bash
    docker-compose -f docker-compose-local.yml up -d
    ```

4.  **실행 상태 확인**
    ```bash
    docker ps
    ```
    *   `mysql`, `redis`, `mosquitto` 컨테이너가 `Up` 상태여야 합니다.
    *   `PORTS` 항목에 `->` 화살표가 보여야 합니다. (예: `0.0.0.0:3306->3306/tcp`)

    > **참고**: `nginx`는 로컬 개발 환경에서 실행되지 않습니다.

5.  **컨테이너 중지 (개발 종료 시)**
    ```bash
    docker-compose -f docker-compose-local.yml down
    ```

---

## ☕ 2. 백엔드 애플리케이션 실행

1.  IDE에서 프로젝트를 엽니다. (`backend/SSETest` 폴더가 아닌 루트 폴더를 열었다면 `settings.gradle` 설정을 확인하세요.)
2.  **Spring Boot 실행 프로파일(Profile) 설정**
    *   로컬 개발 시에는 `local` 프로파일을 사용해야 합니다.
    *   `application.yml` 파일 확인:
        ```yaml
        spring:
          profiles:
            active: local  # prod가 아닌 local로 설정
        ```
    *   또는 IDE 실행 설정(Run Configuration)의 VM Options에 추가:
        ```
        -Dspring.profiles.active=local
        ```

3.  `SseTestApplication` 클래스의 `main()` 메서드를 실행합니다.

---

## 🧪 3. 테스트 코드 실행

테스트 코드는 `@ActiveProfiles("local")` 설정이 되어 있어, 위에서 실행한 Docker 컨테이너와 자동으로 연결됩니다.

1.  인프라(Docker)가 실행 중인지 확인합니다.
2.  `./gradlew test` 명령어를 실행하거나 IDE에서 테스트를 실행합니다.

---

## ⚠️ 주의사항

*   **배포 환경(Production)**: 배포 서버에서는 `docker-compose.yml` (기본 파일)을 사용합니다. 이 파일은 보안을 위해 포트를 외부에 노출하지 않으며, Nginx를 포함합니다.
*   **포트 충돌**: 로컬 PC에서 이미 MySQL(3306)이나 Redis(6379)를 별도로 설치해 사용 중이라면 포트 충돌이 발생할 수 있습니다. 해당 서비스를 중지하고 Docker를 실행하세요.
